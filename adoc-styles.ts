export default class ADocStyles {
  static olua:string = '[upperalpha]'
  static heading2:string = '=='
  static heading3:string = '==='
  static heading1:string = '='
  static codeDelimiter = `
${new Array(100).join('-')}
  `
}